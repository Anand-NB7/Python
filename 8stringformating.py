#if= do some code only if some condition is true
# else do something else

#think tht user is applying for a credit card depending on th e age conditions
age=int(input("Enter your age : "))
if age >=
